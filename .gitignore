# Learn more https://docs.github.com/en/get-started/getting-started-with-git/ignoring-files

# dependencies
node_modules/

# Expo
.expo/
dist/
web-build/
expo-env.d.ts

# Native
.kotlin/
*.orig.*
*.jks
*.p8
*.p12
*.key
*.mobileprovision

# Metro
.metro-health-check*

# debug
npm-debug.*
yarn-debug.*
yarn-error.*
expo-web.*
lifeflow-*.png

# macOS
.DS_Store
*.pem

# local env files
.env*.local

# typescript
*.tsbuildinfo

app-example

# generated native folders
/ios
/android

# Claude Code
.claude/

# Supabase temp
supabase/.temp/

# env files (all variants)
.env
.env.local
.env.*.local
.design-ref/

# Skill manager (find-skills) — tooling local de dev, no código de producto.
# Algunos paths exceden el límite de Windows; no se versionan.
.agents/
skills-lock.json

# Impeccable — config.json/design.json/live/config.json SÍ se versionan (contexto
# de marca compartido); esto es solo estado efímero de sesión local.
.impeccable/hook.cache.json
.impeccable/live/sessions/
.impeccable/config.local.json
# Nombres de archivo con timestamp exceden el límite de path de Windows (mismo
# problema que .agents/ arriba) — snapshots de /impeccable critique, no versionar.
.impeccable/critique/
