# ─── CI — calidad mínima en cada push/PR ──────────────────────────────────────
# lint + typecheck + tests. El export web corre como job aparte para no
# bloquear el feedback rápido de calidad.
name: CI

on:
  push:
    branches: [main, 'launch-**']
  pull_request:

jobs:
  quality:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
      - name: Install (legacy peer deps — react-native-purchases + expo 54)
        run: npm ci --legacy-peer-deps
      - name: Lint
        run: npm run lint
      - name: Typecheck
        run: npm run typecheck
      # Los fixes cuya reversion seria silenciosa: jest no alcanza migraciones
      # SQL, edge functions de Deno ni copy de UI, que es donde han vivido los
      # bugs mas caros de este repo.
      - name: Witness (fixes siguen cableados)
        run: npm run witness
      - name: Tests
        run: npm test -- --ci

  web-export:
    runs-on: ubuntu-latest
    needs: quality
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
      - run: npm ci --legacy-peer-deps
      - name: Expo export (web smoke build)
        run: npx expo export --platform web
