# Maestro Configuration
# Batman QA Suite — Mobile flow runner config

# App under test
appId: com.lifeflow.app

# Default flow settings
---
tags:
  - batman-qa

# Global flow timeout
defaultTimeout: 30000

# Retry failed flows once
retries: 1

# Environment
env:
  BASE_URL: "https://growthplayers.vercel.app"
  TEST_EMAIL: "test-batman@lifeflow.app"
  TEST_PASSWORD: "BatmanQA2026!"
